/**
 * Judge Type Enumeration
 * 
 * Specifies which judge system will be used for code evaluation.
 * 
 * @enum {string}
 */
export enum JudgeType {
  /** Local Judge0 instance */
  LOCAL = 'local'
}

